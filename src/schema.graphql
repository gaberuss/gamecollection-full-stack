# can't import anything until we get our generated folder

type Query {
  info: String!
  inventory: [Game!]!
}

# once the prisma database has been created and deployed
# uncomment and then create the Mutation file
# then create the mutation resolvers

# type Mutation {
#   createGame(name: String!, console: String!, condition: String): Game!
# }
